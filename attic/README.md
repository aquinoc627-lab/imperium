# Attic

In-tree trees that are **not** the product. Frozen scaffolding. Do not add features here.

- `crates/imperium-{crypto,daemon,ffi,policy,runtime,store,sync,voice}`
- `frontend/`
- `docker/`
- Python packages other than `imperium_intent` models
- Specs `02`–`04`

Workspace members that compile: `imperium-core`, `imperium-cli`.
